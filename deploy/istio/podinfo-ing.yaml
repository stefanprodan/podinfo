apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: podinfo
  annotations:
    kubernetes.io/ingress.class: istio
spec:
  rules:
  - host: podinfo.test
    http:
      paths:
      - path: /.*
        backend:
          serviceName: podinfo
          servicePort: 9898
